# Runtime dependencies from the official GVHMR requirements. PyTorch and
# PyTorch3D are installed separately so the image can target RTX 50-series
# GPUs with CUDA 12.8 while keeping the released model weights unchanged.
timm==0.9.12
lightning==2.3.0
hydra-core==1.3.2
hydra-zen
hydra-colorlog
rich
numpy==1.23.5
matplotlib
tensorboardX
opencv-python-headless==4.10.0.84
ffmpeg-python
scikit-image==0.21.0
termcolor
einops
imageio==2.34.1
av==13.0.0
joblib
trimesh
smplx==0.1.28
wis3d
pycolmap
ultralytics==8.2.42
cython-bbox
lapx
yacs
